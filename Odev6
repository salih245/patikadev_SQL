select round(avg(rental_rate),3) from public.film;

select count(*) from public.film where title like 'C%';

select max(length) from public.film where rental_rate = 0.99 ;

select count(distinct replacement_cost) from public.film where length > 150;